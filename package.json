{
  "name": "eventcinemas-watch",
  "version": "1.0.0",
  "private": true,
  "description": "Watches Event Cinemas session listings and emails when new showings are added",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check-once.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.9"
  }
}
